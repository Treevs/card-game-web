mutation UpdateDeck(
    $updateDeckId: ID!
    $name: String
    $cards: [DeckCardInput!]
    $hero: ID
) {
    updateDeck(id: $updateDeckId, name: $name, cards: $cards, hero: $hero) {
        id
    }
}

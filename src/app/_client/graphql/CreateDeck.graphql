mutation CreateDeck(
    $name: String!
    $heroCardId: ID!
    $cards: [DeckCardInput!]!
) {
    createDeck(name: $name, heroCardId: $heroCardId, cards: $cards) {
        id
        name
        heroCardId
        cards {
            cardId
            quantity
        }
    }
}
